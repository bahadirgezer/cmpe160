import java.util.ArrayList;
import java.util.Random;
import java.util.Scanner;
import java.io.File;
import java.io.FileNotFoundException;
import java.io.PrintStream;

public class Main {
    public static void main(String args[]) throws FileNotFoundException {

        for (int i=0; i<1; i++){
            Random random = new Random();
            PrintStream out = new PrintStream("myinput"+i+".txt");

            out.println(random.nextInt());
            
            int multiplier = random.nextInt(10);
            double multiplier2 = random.nextDouble()*10;

            int fee = random.nextInt(500);
            int users = random.nextInt(1000);
            int queries = random.nextInt(1000);

            out.println(fee+" "+users+" "+queries);

            for (int ij=0; ij<users; ij++){
                int dollar = random.nextInt(1000);
                int coin = random.nextInt(1000);
            }

            for (int ij=0; ij<queries; ij++){
                int queryID = random.nextInt(14);
               
                if (queryID == 0){
                    int traderID = random.nextInt(users);
                    double price = random.nextDouble()*multiplier2;
                    double amount = random.nextDouble()*multiplier2;

                    out.println("10 "+traderID+ " "+ String.format("%.3f", price)+ " "+String.format("%.3f", amount));

                } else if (queryID == 1) {
                    int traderID = random.nextInt(users);
                    double amount = random.nextDouble()*multiplier2;
                    
                    out.println("11 "+traderID+ " "+ String.format("%.3f", amount));

                } else if (queryID == 2) {
                    int traderID = random.nextInt(users);
                    double price = random.nextDouble()*multiplier2;
                    double amount = random.nextDouble()*multiplier2;
                    
                    out.println("20 "+traderID+ " "+ String.format("%.3f", price)+ " "+String.format("%.3f", amount));
                
                } else if (queryID == 3) {
                    int traderID = random.nextInt(users);
                    double amount = random.nextDouble()*multiplier2;
                    
                    out.println("21 "+traderID+ " "+ String.format("%.3f", amount));

                } else if (queryID == 4) {
                    int traderID = random.nextInt(users);
                    double amount = random.nextDouble()*multiplier2;
                    
                    out.println("3 "+traderID+ " "+ String.format("%.3f", amount));

                } else if (queryID == 5) {
                    int traderID = random.nextInt(users);
                    double amount = random.nextDouble()*multiplier2;
                    
                    out.println("4 "+traderID+ " "+ String.format("%.3f", amount));

                } else if (queryID == 6) {
                    int traderID = random.nextInt(users);

                    out.println("5 "+ traderID);

                } else if (queryID == 7) {
                    out.println(777);

                } else if (queryID == 8) {
                    double price = random.nextDouble()*multiplier2;

                    out.println("666 "+ String.format("%.3f", price));

                } else if (queryID == 9) {
                    out.println("500");

                } else if (queryID == 10) {
                    out.println("501");
                
                } else if (queryID == 11) {
                    out.println("502");
          
                } else if (queryID == 12) {
                    out.println("505");
                
                } else if (queryID == 13) {   
                    out.println("555");
            
                }


            }//query for loop

        } //test case for loop

    } // main method

} // main class